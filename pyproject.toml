[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "bilevel-autoresearch"
version = "0.3.0"
requires-python = ">=3.10"
description = "A general framework for self-improving research pipelines with bilevel optimization"
readme = "README.md"
license = {file = "LICENSE"}
keywords = ["llm", "ai", "research", "bilevel-optimization", "agents", "pipeline"]
dependencies = [
    "openai>=1.0.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
anthropic = ["anthropic>=0.25.0"]
dev = ["pytest>=8.0", "ruff>=0.3.0"]
trilevel = []   # no new deps; uses base package + upstream domains

[tool.setuptools.packages.find]
where = ["."]
include = ["core*", "domains*", "trilevel_research*"]

[tool.pytest.ini_options]
markers = [
    "trilevel: optional tri-level extension tests",
]

[tool.ruff]
[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501", "E741", "E402"]  # E501: line length, E741: ambiguous var names, E402: .env loading before imports

[tool.rye]
dev-dependencies = [
    "pytest-cov>=7.1.0",
]
